[tool.poetry]
name = "tap-zendesk"
version = "0.0.1"
description = "`tap-zendesk` is a Singer tap for zendesk, built with the Meltano SDK for Singer Taps."
authors = ["Hotglue"]
keywords = [
    "ELT",
    "zendesk",
]

[tool.poetry.dependencies]
python = "<3.11,>=3.7.1"
requests = "^2.25.1"
hotglue-singer-sdk = "^1.0.0"
hotglue-etl-exceptions = "^0.1.0"
# provides `import singer` (resolve_schema_references); NOT `singer-python`
pipelinewise-singer-python = "1.2.0"
typing-extensions = "*"

[build-system]
requires = ["poetry-core>=1.0.8"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
tap-zendesk = "tap_zendesk.tap:TapZendesk.cli"

[tool.tox]
requires = [
    "tox>=4.43",
    "tox-uv",
]
env_list = [
    "lint",
    "typing",
    "3.10",
]

[tool.tox.env_run_base]
description = "run tests on Python {env_name}"
runner = "uv-venv-runner"
pass_env = [
    "TAP_ZENDESK_*",
]
deps = ["pytest>=9"]
commands = [ [ "pytest", { replace = "posargs", default = [ "tests" ], extend = true } ] ]

[tool.tox.env.lint]
description = "run ruff linter and formatter check"
skip_install = true
deps = ["ruff>=0.15"]
commands = [
    ["ruff", "check", "."],
    ["ruff", "format", "--check", "."],
]

[tool.tox.env.typing]
description = "type checks"
runner = "uv-venv-runner"
deps = [
    "mypy>=1.19.1",
    "ty>=0.0.18",
    "types-requests",
]
commands = [
    [ "mypy", { replace = "posargs", default = [ "tap_zendesk", "tests" ], extend = true } ],
    [ "ty", "check", { replace = "posargs", default = [ "tap_zendesk", "tests" ], extend = true } ],
]
